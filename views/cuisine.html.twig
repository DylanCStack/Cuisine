<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Cuisine Finder</title>
    </head>
    <body>
        <h1>Cuisine Finder</h1>
        <h2>{{cuisine.getName}}
        {%if restaurants is not empty%}
            <ul>
            {% for restaurant in restaurants %}
                <li><a href='/restaurant/{{cuisine.getName}}/{{restaurant.getName}}'>{{restaurant.getName}}</a></li>
            {%endfor%}
            </ul>
        {%endif%}
        <form class="restaurant" action="/cuisine/{{cuisine.getName}}" method="post">
            <label for="restaurant">Add a new restaurant</label>
            <input type="text" name = "restaurant">

            <label for="address">Address</label>
            <input type="text" name = "address">

            <label for="website">Website</label>
            <input type="text" name = "website">

            <label for="phone">Phone</label>
            <input type="text" name = "phone">

            <button type = "submit">Add</button>
        </form>
        <form class="restaurant" action="/deleteAllByCuisine/{{cuisine.getName}}" method="post">
            <button type = "submit">Purge</button>
        </form>

    </body>
</html>
